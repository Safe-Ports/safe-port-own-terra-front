VITE_API_URL=https://api.your-domain.com/api/v1
VITE_SENTRY_DSN=
# El asistente usa respuestas locales simuladas solo para revisar el frontend.
# En producción debe quedar en false (o sin definir) y apuntará a POST /chat-ai.
VITE_AI_DEMO_MODE=false
